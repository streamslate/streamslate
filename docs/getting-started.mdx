---
title: Getting Started
description: Learn how to set up and use StreamSlate for live streaming and content creation
---

# Getting Started with StreamSlate

StreamSlate is a desktop PDF-annotation app designed specifically for live streamers and YouTubers who need to annotate PDFs during their broadcasts.

## Prerequisites

Before you begin, ensure you have the following installed:

- **Node.js** (version 20 or higher)
- **Rust** (latest stable version)
- **npm** or **yarn** package manager

### System Requirements

- **macOS**: 10.15 (Catalina) or later
- **Windows**: Windows 10 or later
- **Linux**: Ubuntu 20.04 or equivalent

## Installation

### Option 1: Download Release (Recommended)

1. Visit the [releases page](https://github.com/streamslate/streamslate/releases)
2. Download the latest version for your operating system
3. Install the application following your OS-specific instructions

### Option 2: Build from Source

1. Clone the repository:

   ```bash
   git clone https://github.com/streamslate/streamslate.git
   cd streamslate
   ```

2. Install dependencies:

   ```bash
   npm install
   ```

3. Build and run the application:
   ```bash
   npm run tauri:dev
   ```

## Quick Start

### 1. Launch StreamSlate

After installation, launch StreamSlate from your applications folder or by running:

```bash
npm run tauri:dev
```

### 2. Open a PDF

1. Click **File > Open PDF** or use the keyboard shortcut `Cmd+O` (macOS) / `Ctrl+O` (Windows/Linux)
2. Select your PDF file from the file dialog
3. The PDF will load in the main viewer

### 3. Start Annotating

- **Text Tool**: Click the text icon and click anywhere on the PDF to add text annotations
- **Drawing Tool**: Select the pen tool to draw freehand annotations
- **Highlighter**: Use the highlighter to emphasize important content
- **Underline and Strikethrough**: Draw visual line marks over existing PDF text using annotation geometry
- **Shapes**: Add rectangles, circles, and arrows to highlight areas

Underline and strikethrough annotations are visual marks placed on the page.
They do not perform OCR or extract semantic text from the PDF.

### 4. Presenter Mode

For live streaming, enable Presenter Mode:

1. Click the **Presenter Mode** button or press `F11`
2. A separate window opens that you can capture in your streaming software
3. The presenter window displays your annotations in real-time
4. Configure window position and size to fit your streaming layout

## Streaming Integration

### OBS Studio Setup

1. Add a **Window Capture** source in OBS
2. Select the StreamSlate Presenter window
3. Position and resize as needed in your scene
4. Your PDF annotations will now appear in your stream

You can also ingest StreamSlate through NDI or Syphon native output when those
build-time integrations are enabled.

### OBS WebSocket Direct Control

StreamSlate can connect to a local OBS Studio instance over OBS WebSocket v5 for
production control. This is separate from capturing the StreamSlate presenter
window or native video output.

1. In OBS, enable **Tools > WebSocket Server Settings**
2. Keep the default server port `4455`, or update StreamSlate's OBS integration
   settings to match your OBS host and port
3. In StreamSlate, open **Settings > Output** and click **Connect** under OBS
4. Use the OBS scene selector, source visibility toggles, and recording or
   streaming buttons from StreamSlate

### Stream Deck Automation

The official Stream Deck SDK v2 plugin lives under `plugins/streamdeck` for
local development and validation. It is not published on Elgato Marketplace,
and hardware or Stream Deck Mobile verification is still tracked separately.
Developers can follow the
[Stream Deck plugin development guide](/docs/streamdeck-plugin). For released
builds that do not include the plugin yet, use Stream Deck actions that can call
local WebSocket commands, or bridge through another automation tool that can
send JSON to the StreamSlate WebSocket API.

See the [Local API](/docs/api) and [V2 API contract](/docs/api-v2-contract) for
the supported command list.

- **Next Page**: send `{"type":"NEXT_PAGE"}`
- **Previous Page**: send `{"type":"PREVIOUS_PAGE"}`
- **Clear Annotations**: send `{"type":"CLEAR_ANNOTATIONS"}`
- **Toggle Presenter**: send `{"type":"TOGGLE_PRESENTER"}`

## Keyboard Shortcuts

| Shortcut       | Action                |
| -------------- | --------------------- |
| `Cmd/Ctrl + O` | Open PDF              |
| `Cmd/Ctrl + S` | Save annotations      |
| `F11`          | Toggle Presenter Mode |
| `Cmd/Ctrl + Z` | Undo last annotation  |
| `Cmd/Ctrl + Y` | Redo annotation       |
| `Space`        | Pan tool              |
| `T`            | Text tool             |
| `P`            | Pen tool              |
| `H`            | Highlighter           |

## Configuration

### Settings

Access settings through **File > Preferences**:

- **Appearance**: Choose between light and dark themes
- **Annotations**: Set default colors, line weights, and fonts
- **Streaming**: Configure presenter mode display options
- **Shortcuts**: Customize keyboard shortcuts

### WebSocket API

StreamSlate exposes a WebSocket API on port `11451` for external integrations
and automation:

```javascript
const ws = new WebSocket("ws://127.0.0.1:11451");

// Go to a 1-based PDF page number.
ws.send(
  JSON.stringify({
    type: "GO_TO_PAGE",
    page: 5,
  })
);

// Clear all annotations.
ws.send(
  JSON.stringify({
    type: "CLEAR_ANNOTATIONS",
  })
);
```

## Troubleshooting

### Common Issues

**PDF won't open**

- Ensure the PDF file isn't corrupted
- Check that the file isn't password protected
- Verify you have read permissions for the file

**Presenter mode not showing**

- Check that your streaming software is capturing the correct window
- Ensure the presenter window isn't minimized
- Try toggling presenter mode off and on again

**Annotations not saving**

- Verify you have write permissions in the chosen save location
- Check available disk space
- Ensure the application isn't running in read-only mode

### Getting Help

- **Documentation**: Visit our [full documentation](https://streamslate.app/docs)
- **Issues**: Report bugs on [GitHub Issues](https://github.com/streamslate/streamslate/issues)
- **Discord**: Join our [community Discord](https://discord.gg/streamslate)
- **Email**: Contact support at support@streamslate.app

## Next Steps

- Explore the [Advanced Features](/docs/advanced-features) guide
- Learn about [Custom Integrations](/docs/integrations)
- Check out [Video Tutorials](/docs/tutorials) for visual walkthroughs
- Join the community to share tips and get help

---

_StreamSlate is open source and licensed under GPL-3. Contributions are welcome!_
